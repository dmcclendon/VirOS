# TODO: this directory is scheduled for demolition

all:
	ln -s ../scripts/vsys .
	ln -s ../checkport/checkport .
	ln -s ../splitter/splitter .

clean:
	rm -f vsys 
	rm -f checkport
	rm -f splitter

install:

uninstall:
